# ~/.tmuxinator/project_crazyflie_basic.yml

<%
%>

attach: true
root: ./
startup_window: foxglove
# on_project_exit: pkill -9 -f "gazebo"
windows:
  - foxglove:
      layout:
      panes:
        - ros2 launch foxglove_bridge foxglove_bridge_launch.xml use_compression:=true client_topics_whitelist:='["/drone2/object_markers", "/drone2/front_camera/yolo/dbg_image"]'